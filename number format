function formatNumber(num) { //chop off 1 zero and devide by 10
  if (num >= 1000000) {
    return Math.floor(num / 100000)/10 + "M";
  } else if (num >= 1000) {
    return Math.floor(num / 100)/10 + "K";
  } else {
    return num;
  }
}
  
